<div class="product-item text-center">
	<a href="{{path('product', {'slug': product.slug})}}"><img style="height: 200px; margin-top: 50px; width: 200px"
			src="../uploads/{{ product.media }}" alt={{product.title}}></a>
	{# {% include "products/modal.html.twig" %} #}

	<h5>{{product.title}}</h5>
	<span class="product-price">{{(product.price /100)|number_format(2, ',', '.')}}
		€</span>

	{# <p class="card-text" style="font-size: 12px;">{{product.description}}</p> #}


	<div class="mt-3">
		<ul class="">
			<li class="" style="list-style: none;">
				<a href="{{path('product', {'slug': product.slug})}}" class="">Voir</a>
				<a href="{{path('cart-add', {'id': product.id} )}}" class="">
					<i class="fas fa-shopping-cart mr-1"></i>Ajouter au panier</a>
			</li>
		</ul>

	</div>
</div>